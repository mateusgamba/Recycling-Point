export interface Item {
  id: number,
  name: string,
  image_url: string
}

export interface Point {
  name: string,
  email: string,
  whatsapp: string,
  zip: string,
  street: string,
  number: string,
  city: string,
  province: string,
  country: string,
}

export interface Countries {
  id: number,
  name: string,
}